# Optional environment overrides. Telegram uses the Docker secret
# configured by DASHI_TELEGRAM_TOKEN_FILE, not this file.

# Optional voice secret, only when bridge.config.json has voice.provider="groq".
# Prefer GROQ_API_KEY_FILE when your deployment can mount another secret.
# GROQ_API_KEY=
