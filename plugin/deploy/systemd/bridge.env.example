# Optional environment overrides. Telegram uses systemd LoadCredential from
# /etc/dashi-codex-bridge/telegram-token, not this file.

# Optional, only when bridge.config.json has voice.provider="groq".
# GROQ_API_KEY=
